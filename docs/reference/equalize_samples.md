# Equalize samples when using replicates

Equalize samples when using replicates

## Usage

``` r
equalize_samples(df_1, df_rep)
```

## Arguments

- df_1:

  Data table of the specified sample

- df_rep:

  Data table of the replicate sample
