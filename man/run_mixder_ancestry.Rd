% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_mixder_ancestry.R
\name{run_mixder_ancestry}
\alias{run_mixder_ancestry}
\title{run MixDeR using the CLI; ancestry prediction}
\usage{
run_mixder_ancestry(
  sample_manifest = NULL,
  sample = NULL,
  replicate = NULL,
  sample_reports = getwd(),
  output = "output",
  refpath = NULL,
  refs = NULL,
  mixdeconv = TRUE,
  uncond = TRUE,
  cond = FALSE,
  sets = 10,
  dynamicAT = 0.015,
  staticAT = 10,
  minimum_snps = 6000,
  A1_threshold = 0.99,
  A2_threshold = 0.6,
  minor_contrib_threshold = FALSE,
  keep_bins = TRUE,
  snps,
  pcagroups
)
}
\arguments{
\item{sample_manifest}{path to sample manifest file (default=NULL)}

\item{sample}{If running individual sample, specify the sample ID; requires sample_manifest=NULL (default=NULL)}

\item{replicate}{If running individual sample along with a replicate, specify the replicate ID; requires sample_manifest=NULL (default=NULL)}

\item{sample_reports}{Path to directory of Sample Reports or CSV file of mixture genotypes (default=current directory)}

\item{output}{name of output directory, will be outputted into the sample reports directory (default="output")}

\item{refpath}{Path to directory containing reference genotypes (default=NULL)}

\item{refs}{list of sample IDs to use as reference for conditioning, can specify more than one (default=NULL, example: list("Ref1", "Ref2"))}

\item{mixdeconv}{whether to run mixture deconvolution (default=TRUE)}

\item{uncond}{run (or use) an unconditioned mixture deconvolution (default=TRUE)}

\item{cond}{run (or use) a conditioned mixture deconvolution (default=FALSE)}

\item{sets}{number of bins to divide SNPs into (default=10)}

\item{dynamicAT}{dynamic analytical threshold applied to data (default=0.015)}

\item{staticAT}{static analytical threshold applied to data (default=10)}

\item{minimum_snps}{minimum number of SNPs used to generate profile (default=6000)}

\item{A1_threshold}{Allele 1 probability threshold (default=0.99)}

\item{A2_threshold}{Allele 2 probability threshold (default=0.60)}

\item{minor_contrib_threshold}{Whether to apply the allele 1 probability threshold to the minor contributor, regardless of the minimum number of SNPs (default=FALSE)}

\item{keep_bins}{Use existing binned SNP data, if exists (default=TRUE)}

\item{snps}{SNPs to use for ancestry prediction (either ancestry only or all SNPs)}

\item{pcagroups}{How to color PCA plots (superpopulations and/or subpopulations)}

\item{filter_missing}{Whether to remove SNPs with a missing value for the allele 2 (default=FALSE)}
}
\description{
run MixDeR using the CLI; ancestry prediction
}
